"""engagements models — to be implemented.

The underlying engagements tables are already provisioned in PostgreSQL via the
srj-mcp bootstrap migration (2026-06-23). Django models will use
managed=False to map to them once defined.
"""
