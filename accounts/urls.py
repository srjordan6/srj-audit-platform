"""URLs for the accounts app. Placeholder; views will be added as
buyer authentication flows are built (Part A §2.6)."""
from django.urls import path

app_name = 'accounts'

urlpatterns = [
    # path('login/',  views.login,  name='login'),
    # path('logout/', views.logout, name='logout'),
    # path('signup/', views.signup, name='signup'),
]
