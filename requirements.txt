# SRJ AI Audit Platform — Tier 1 dependencies
# Locked versions per Part A v1.1 Appendix B.1.
# Security refresh 2026-07-19: Django 5.1.15, pypdf 6.13.3, WeasyPrint 68.0
# (62 CVEs closed — see Security_Scan_Report_2026-07-19.md). django-allauth
# removed (unused; zero imports anywhere in the codebase). Dev/test tools
# moved to requirements-dev.txt so they don't ship in the production image.

Django==5.1.15
psycopg[binary]==3.2.3
django-environ==0.11.2
gunicorn==23.0.0
whitenoise==6.8.2

# Background jobs
redis==5.2.1
django-rq==2.10.2
rq==1.16.2

# Bot protection (Turnstile server-side verification)
requests>=2.32.0

# PDF generation
# WeasyPrint 68.0 confirmed working on Render 2026-07-20 (generated a
# 286 KB report end-to-end). An earlier "68 segfaults" diagnosis was
# wrong — generation simply takes ~3 minutes because the Claude narrative
# call runs inside it, and the check happened before it finished.
# Defence in depth on its two SSRF CVEs: running 68.0 (which patches
# PYSEC-2026-2034) AND the deny-all url_fetcher in reports/generator.py
# (which covers PYSEC-2026-3412, still unpatched upstream).
WeasyPrint==68.0
pydyf==0.12.1
pypdf==6.13.3
cryptography>=42.0

# S3-compatible storage (Backblaze B2)
boto3==1.35.0
django-storages==1.14.4

# Payments
stripe==11.4.0

# Email (Postmark)
postmarker==1.0

# Monitoring
sentry-sdk==2.19.0

# Frontend
django-htmx==1.21.0
django-crispy-forms==2.3
crispy-bootstrap5==2024.10

# AI narrative analysis
anthropic>=0.40.0
