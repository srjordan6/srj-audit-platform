# SRJ AI Audit Platform — Tier 1 dependencies
# Locked versions per Part A v1.1 Appendix B.1.
# Security refresh 2026-08-16 (Aikido): Django 5.2.17 LTS (5.1 is EOL),
# gunicorn 26.1.0, django-storages 1.14.6, pypdf 6.16.1, requests 2.34.2,
# cryptography pinned explicitly. Prior refresh 2026-07-19 notes below.
# Security refresh 2026-07-19: Django 5.1.15, pypdf 6.13.3, WeasyPrint 68.0
# (62 CVEs closed — see Security_Scan_Report_2026-07-19.md). django-allauth
# removed (unused; zero imports anywhere in the codebase). Dev/test tools
# moved to requirements-dev.txt so they don't ship in the production image.

Django==5.2.17
psycopg[binary]==3.2.3
django-environ==0.11.2
gunicorn==26.1.0
whitenoise==6.8.2

# Background jobs
redis==5.2.1
django-rq==2.10.2
rq==1.16.2

# Bot protection (Turnstile server-side verification)
requests==2.34.2

# PDF generation
# WeasyPrint 70.0. Upgraded from 68.0 on 2026-09-15 to close two MODERATE
# advisories, both fixed in 70.0:
#
#   CVE-2026-55073 / GHSA-jf6q-chmf-3h3v  SSRF. write_pdf() ignored the
#     document url_fetcher on the xmp_metadata and attachments channels
#     and built a fresh default fetcher. The deny-all url_fetcher in
#     reports/generator.py did NOT mitigate this - being bypassed was the
#     vulnerability. We were unaffected only because write_pdf() is called
#     with no arguments. Keep the deny-all fetcher regardless: it is what
#     blocks remote and file:// resource loads on the normal render path.
#
#   CVE-2026-49452 / GHSA-jhhc-3hcp-qhm5  CSS injection via presentational
#     hints. Unrelated to url_fetcher. We were unaffected because
#     presentational_hints defaults to False.
#
# Layout validated before the bump: tier1_snapshot.html rendered on both
# 68.0 and 70.0 produced identical page counts, page sizes and per-page
# text, including an inflated 57-page run exercising the page-break path.
# pydyf 0.12.1 is compatible with 70.0 and does not need to move.
WeasyPrint==70.0
pydyf==0.12.1
pypdf==6.16.1

# S3-compatible storage (Backblaze B2)
boto3==1.43.93
django-storages==1.14.6

# Payments
stripe==11.4.0

# Email (Postmark)
postmarker==1.0

# Monitoring
sentry-sdk==2.19.0

# Frontend
django-htmx==1.21.0
django-crispy-forms==2.3
crispy-bootstrap5==2024.10

# AI narrative analysis
anthropic>=0.40.0

# Explicit pin: transitive dep flagged by scanner (out-of-bounds read in older lines)
cryptography==50.0.0
