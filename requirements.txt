# SRJ AI Audit Platform — Tier 1 dependencies
# Locked versions per Part A v1.1 Appendix B.1.
# Security refresh 2026-08-16 (Aikido): Django 5.2.17 LTS (5.1 is EOL),
# gunicorn 26.1.0, django-storages 1.14.6, pypdf 6.16.1, requests 2.34.2,
# cryptography pinned explicitly. Prior refresh 2026-07-19 notes below.
# Security refresh 2026-07-19: Django 5.1.15, pypdf 6.13.3, WeasyPrint 68.0
# (62 CVEs closed — see Security_Scan_Report_2026-07-19.md). django-allauth
# removed (unused; zero imports anywhere in the codebase). Dev/test tools
# moved to requirements-dev.txt so they don't ship in the production image.

Django==5.2.17
psycopg[binary]==3.2.3
django-environ==0.11.2
gunicorn==26.1.0
whitenoise==6.8.2

# Background jobs
redis==5.2.1
django-rq==2.10.2
rq==1.16.2

# Bot protection (Turnstile server-side verification)
requests==2.34.2

# PDF generation
# WeasyPrint is held at 68.0. 68.0 renders a full Tier 1 report correctly
# (verified 2026-07-20, 286 KB end to end); an earlier "68 segfaults"
# diagnosis was wrong, generation simply takes ~3 minutes because the
# Claude narrative call runs inside it.
#
# 68.0 carries two open MODERATE advisories. Neither is reachable from
# this codebase today, but NOT for the reasons previously recorded here.
# Verified against OSV 2026-09-15:
#
#   CVE-2026-55073 / GHSA-jf6q-chmf-3h3v  SSRF, fixed in 70.0.
#     write_pdf() IGNORES the document url_fetcher on the xmp_metadata and
#     attachments channels and builds a fresh default fetcher. The deny-all
#     url_fetcher in reports/generator.py does NOT mitigate this - being
#     bypassed is the vulnerability. We are unaffected only because
#     generator.py calls write_pdf() with no arguments, so neither channel
#     is used. Passing xmp_metadata= or attachments= would expose us.
#
#   CVE-2026-49452 / GHSA-jhhc-3hcp-qhm5  CSS injection, affects <= 68.1.
#     Unescaped attribute values embedded into CSS when HTML presentational
#     hints are enabled. Unrelated to url_fetcher. We are unaffected because
#     presentational_hints defaults to False and we never enable it.
#
# 70.0 fixes both. The upgrade is wanted; it is held only until a full
# report is generated on 70.0 and compared against a known-good PDF,
# because major bumps change CSS layout. See SECURITY.md.
WeasyPrint==68.0
pydyf==0.12.1
pypdf==6.16.1

# S3-compatible storage (Backblaze B2)
boto3==1.43.78
django-storages==1.14.6

# Payments
stripe==11.4.0

# Email (Postmark)
postmarker==1.0

# Monitoring
sentry-sdk==2.19.0

# Frontend
django-htmx==1.21.0
django-crispy-forms==2.3
crispy-bootstrap5==2024.10

# AI narrative analysis
anthropic>=0.40.0

# Explicit pin: transitive dep flagged by scanner (out-of-bounds read in older lines)
cryptography==50.0.0
