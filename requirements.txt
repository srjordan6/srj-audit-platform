# SRJ AI Audit Platform — Tier 1 dependencies
# Locked versions per Part A v1.1 Appendix B.1.
# Security refresh 2026-07-19: Django 5.1.15, pypdf 6.13.3, WeasyPrint 68.0
# (62 CVEs closed — see Security_Scan_Report_2026-07-19.md). django-allauth
# removed (unused; zero imports anywhere in the codebase). Dev/test tools
# moved to requirements-dev.txt so they don't ship in the production image.

Django==5.1.15
psycopg[binary]==3.2.3
django-environ==0.11.2
gunicorn==23.0.0
whitenoise==6.8.2

# Background jobs
redis==5.2.1
django-rq==2.10.2
rq==1.16.2

# Bot protection (Turnstile server-side verification)
requests>=2.32.0

# PDF generation
# WeasyPrint pinned to 62.3 deliberately. 68.0 was deployed 2026-07-19 and
# hard-crashed the report worker on Render (process died with no Python
# traceback = native segfault; WeasyPrint 61+ wants newer pango/cairo than
# Render's image ships). Its two CVEs are SSRF-class issues in external
# URL fetching — neutralized instead by the deny-all url_fetcher in
# reports/generator.py, which is a stronger control than the version bump
# since our HTML never legitimately fetches anything remote.
# Re-test 68.x only against Render's actual image, never a local sandbox.
WeasyPrint==62.3
pydyf==0.10.0
pypdf==6.13.3
cryptography>=42.0

# S3-compatible storage (Backblaze B2)
boto3==1.35.0
django-storages==1.14.4

# Payments
stripe==11.4.0

# Email (Postmark)
postmarker==1.0

# Monitoring
sentry-sdk==2.19.0

# Frontend
django-htmx==1.21.0
django-crispy-forms==2.3
crispy-bootstrap5==2024.10

# AI narrative analysis
anthropic>=0.40.0
