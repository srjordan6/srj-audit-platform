# Development / test tools only — NOT installed in production.
# Install locally with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

django-debug-toolbar==8.0.0
pytest==9.1.1
pytest-django==4.14.0
factory-boy==3.3.3
